{
  "name": "baby-meal-gen",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "lint": "echo 'lint not configured'"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
